import random
count = 0

#iterate 100 times
for i in range(1,101):
    #random value
    value = random.randint(0,20)
    #test if it is divisible by 3
    if value % 3 ==:
        count += 1

#output
print("Number of values divisible by 3 = " + str(count))
    
